    <section id="main" class="main">

        <div class="container">
            <h4>ADD BRAND</h4>
                    
             <form  action="/admin/update-brand/{{brand._id}}" method="post" enctype="multipart/form-data" onsubmit="return validationn()" >
            <div class="mb-3">
                <label for="inputBrand" class="col-sm-2 col-form-label">Brand name</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" name="brandname" onkeyup="brand()"
                     id="Brand" value="{{brand.brandname}}"><span id="brand" class="text-danger"></span>
                </div>
                 <label for="inputNumber" class="col-sm-2 col-form-label">Brand thumbnail</label>
                 <div class="col-sm-10">
                    <img src="" style="height: 50px; width: 45px; margin-bottom: 20PX;"  id="imgView" ></td>
                    <input class="form-control" type="file" id="formFile" name="thumbnail" onchange="viewImg(event)">
                </div>

                  <label for="inputNumber" class="col-sm-2 col-form-label">Status</label>
                   <div class="col-sm-10">
                  <input type="text" class="form-control" name="status" value="{{brand.status}}">
                 </div>
                 <input type="text" class="form-control" name="id" value="{{brand._id}}"hidden>
            </div>

            <div class="col-sm-10 text-center">
                <button type="submit" class="btn btn-primary">UPDATE BRAND</button>
            </div>

            </form>

        </div>
    </section>
</body>

<script src="/assets2/js/main.js"></script>

 <script>
        var errorName = document.getElementById('category')
         var errorbrand = document.getElementById('brand')
        
       
           

         function brand() {
            const name = document.getElementById('Brand').value;
            if (name == "") {
                errorbrand.innerHTML = '*required'
                return false
            }
            if (!name.match(/^[a-zA-Z0-9 ]*$/)) {
                errorbrand.innerHTML = '*'
                return false
            }
            if (name.match(/^[ ]*$/)) {
                errorbrand.innerHTML = '*required'
                return false
            }
            errorbrand.innerHTML = null
            return true
        }


         function validation() {
            if (!category()) {
                return false
            }
            return true
        } 


         function validationn() {
            if (!brand()) {
                return false
            }
            return true
        } 


         
     function viewImg(event){
         document.getElementById("imgView").src=URL.createObjectURL(event.target.files[0])
    }


    </script>


    